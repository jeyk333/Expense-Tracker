import React from "react";
import { Typography } from "antd";

const { Title } = Typography;

export const Header = () => {
  return <Title style={{ textAlign: "center" }}>Expense Tracker</Title>;
};
